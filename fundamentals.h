#ifndef _FUNDAMELNTAL_H_ // Check if the header is not defined yet.
#define _FUNDAMENTAL_H_ // Define the header to ensure that it is not defined again.
#include <stdio.h> // For standard input/output
#include <stdlib.h> // For standard library for atoi (converting string to integer)
#include <string.h> // For string functions
void fundamentals(void); // Declare function prototype
void fundamentalsV2(void);
void fundamentalsV3(void);
#endif // End of the header 